import 'package:flutter/material.dart';

class PostreCards extends StatelessWidget {
   
  const PostreCards({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('PostreCards'),
      ),
    );
  }
}